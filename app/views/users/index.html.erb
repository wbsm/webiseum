<div class="row">
  <div class="col-lg-6">
    <%= form_tag(search_users_path, method: "get") do %>
      <div class="input-group">
        <span class="input-group-btn">
          <%= submit_tag "Buscar", :type => "submit", :class => "btn btn-default" %>
        </span>
        <input type="text" class="form-control" name="search_field">
      </div>
    <% end %>
  </div>
  
  <div class="col-lg-6">
    <div class="pull-right">
      <%= button_to "Novo Usuário", { :action => "new" }, :class => "btn btn-default", :method => :get %>
    </div>
  </div>
</div>

</br>
<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <th>Full Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.full_name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Exibir', user %></td>
        <td><%= link_to 'Editar', edit_user_path(user) %></td>
        <td><%= link_to 'Excluir', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
